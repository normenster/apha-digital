'use strict';

/**
 * feedback-error controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::feedback-error.feedback-error');
